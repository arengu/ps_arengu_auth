name: Release version

on:
  push:
    tags:
      - '*'

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      BUILD_FILENAME: ${{ github.event.repository.name }}_${{ github.ref }}.zip

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
      with:
        persist-credentials: false

    - name: Validate composer
      run: composer validate

    - name: Cache dependencies
      id: composer-cache
      uses: actions/cache@v2
      with:
        path: vendor
        key: ${{ runner.os }}-php-${{ hashFiles('**/composer.lock') }}
        restore-keys: |
          ${{ runner.os }}-php-

    - name: Install dependencies
      if: steps.composer-cache.outputs.cache-hit != 'true'
      run: composer install --no-dev --classmap-authoritative --no-progress --no-suggest

    - name: Compress directory
      run: |
        REPO_DIRNAME=$(basename $(pwd))
        cd ..
        zip -9 -q -r "$BUILD_FILENAME" "$REPO_DIRNAME" --exclude "*/.*"

    - name: Create release
      id: create-release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref }}
        release_name: ${{ github.ref }}

    - name: Upload release asset
      id: upload-release-asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create-release.outputs.upload_url }}
        asset_path: ../${{ env.BUILD_FILENAME }}
        asset_name: ${{ env.BUILD_FILENAME }}
        asset_content_type: application/zip
